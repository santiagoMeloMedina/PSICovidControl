
import { environment as ENV } from './environment';

export const environment = {
  production: true,
  PETITION: ENV.PETITION,
  MENU: ENV.MENU,
  FUNCTION: ENV.FUNCTION,
  ACTION: ENV.ACTION,
  AUTHENTICATION: ENV.AUTHENTICATION,
  ROUTING: ENV.ROUTING,
  VALUE: ENV.VALUE,
  TITLES: ENV.TITLES,
  HTTP_CODES: ENV.HTTP_CODES
};
